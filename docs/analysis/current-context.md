# Текущий контекст аналитической работы

> [Индекс](README.md) · [`ART-00`](00-source-and-decision-register.md) · [Governance](governance.md) · [Статус проекта](../project-status.md)

| Поле | Значение |
|---|---|
| Дата | 2026-08-31 |
| Стадия | Аналитический пакет утверждён; Gate 2 реализации готов к повторному независимому review |
| Текущая предметная правка | `SRC-050` / `DEC-058`: решения третьего круга о критических ошибках и хранении MVP-данных |
| Последняя утверждённая версия `ART-00` | v2.4, `APPROVED_BY_OWNER` (`SRC-051`) |
| Текущий gate | Gate 2: безопасная загрузка корпуса, `READY_FOR_INDEPENDENT_REVIEW` |
| Следующий шаг | Повторное read-only review Gate 2 и отдельное утверждение владельца |

## Что уже нормативно установлено

- Продукт помогает проверить, рассматривался ли товар ранее, возвращает кандидатов и историю; окончательное решение принимает человек.
- Результат запроса: `NEW / SAME_PRODUCT / UNCERTAIN`; отношение к отличающемуся кандидату: `VARIANT / UNRELATED`; для `SAME_PRODUCT` — `N/A`. `ANALOG` исключён.
- В MVP действует одна продуктовая роль `CHECK`. Менеджер и руководитель работают во внешнем процессе; поставщик не участвует в продукте.
- `NEW` — результат поиска, а не статус записи. При `NEW` пользователь может вручную вне продукта внести товар в Google Sheets. Продукт в MVP статусы только показывает и не создаёт или не меняет записи.
- Начальный корпус MVP — ногтевой поднабор. Детальные правила сопоставления действуют только для «Накладных ногтей»; вне поднабора поиск возвращает только `NEW` или `UNCERTAIN`, а для остальных категорий поднабора правила ещё не определены.
- Текущая таблица — источник AS-IS, а не спецификация будущей системы. Финансовые формулы вне scope.
- Полный корпус включает рабочую таблицу, действующий ассортимент, архивы рассмотрений и доступные фотографии; на первом этапе допустима однократная выгрузка.
- Подготовленный корпус MVP загружают полным снимком XLSX вне пользовательского интерфейса. `product_id` назначает команда аналитиков и сохраняет при перезагрузках; номер строки — локатор снимка. Поиск использует одно основное встроенное фото.
- Бизнес-требования находятся в [`ART-01`](01-vision-and-scope.md), бизнес-правила — в [`ART-05`](05-business-rules.md), сценарии и функциональные требования — в [`ART-06`](06-user-scenarios-and-functional-requirements.md) v0.7 `DRAFT`.
- Третий круг по `ART-06`–`ART-10` принят `DEC-058`: повреждённый/нечитаемый XLSX и повтор `product_id` блокируют замену корпуса; строки без категории или основного фото исключаются. JSON-отчёты хранятся 90 дней.

## Как продолжать

- `FAST_PATH` не читает этот файл без предметной зависимости.
- Для нового аналитического шага сначала используй настоящий контекст, затем выбери только нужный реестр или артефакт по [индексу](README.md).
- Источники, решения, пробелы и история разделены внутри [`ART-00`](00-source-and-decision-register.md); не загружай весь пакет целиком.
- Действующие режимы работы, матрица моделей и правила крупных источников находятся в [governance](governance.md).
- Обязательный состав и границы каждого `ART-*` находятся в [контрактах артефактов](artifact-contracts.md).
- Исторические `docs/discovery/*`, `docs/project-plan.md` и `docs/architecture.md` используются только как источники гипотез.

## Открытые границы

- `GAP-006`: полный набор полей, правила хранения жизненных статусов и детализация аудита.
- `GAP-007`: постоянная классификация, аудит и правила обезличивания.
- Остальные действующие пробелы — в [`registers/gaps-and-package.md`](registers/gaps-and-package.md).
