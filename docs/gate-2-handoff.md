# Handoff: Gate 2 безопасной загрузки корпуса

> Актуально на 2026-09-01. Этот файл фиксирует технический результат и не является owner approval.

## Текущий статус

Gate 2 находится в `PENDING_OWNER_APPROVAL`. Реализация и повторный независимый read-only review завершены; Gate 3 нельзя начинать до отдельного явного решения владельца.

Работа сохранена локальным коммитом `eb8bcd6 feat: make Gate 2 imports recoverable` в ветке `codex/gate-2-import-reliability`. На момент фиксации рабочее дерево чистое. `main` и удалённый репозиторий не менялись.

## Что изменено

- Миграция `0004_import_operations` добавляет устойчивый журнал `import_operations`.
- Импорт сначала фиксирует `operation_id` в журнале. Если это не удалось, команда завершает работу с кодом `4` и не начинает staging.
- Активация сохраняет связь версии корпуса с операцией в одной транзакции. При потерянном подтверждении после `COMMIT` сервис сверяет журнал и не удаляет уже активный корпус.
- Staging, обычный импорт и reconciliation используют общую файловую блокировку. PostgreSQL использует транзакционную advisory-блокировку при активации.
- Очистка удаляет каталоги неактивных версий до удаления их записей из БД. Невыполненная очистка остаётся повторяемой и даёт `CLEANUP_PENDING`.
- JSON-отчёт хранится по постоянному пути `reports/<operation_id>.json`. Его полезная нагрузка есть в журнале, поэтому отчёт можно восстановить после сбоя записи.
- Добавлена команда `scripts/reconcile_imports.py` для одной операции или всех незавершённых операций.

## Проверки и review

- `make test`: `41 passed, 1 skipped`. PostgreSQL integration-тест запускается только при явном `GATE2_TEST_DATABASE_URL`.
- `make lint`, `make check-delivery`, `git diff --check`: PASS.
- В контролируемом изолированном PostgreSQL Compose-проекте применены миграции `0001`–`0004` с нуля; затем integration-тест подтвердил активацию и `CLEANUP_COMPLETE`.
- Повторный независимый review: `PASS`; Critical и Major отсутствуют.

Тесты покрывают потерянное подтверждение после `COMMIT`, сбой публикации отчёта с повтором, ошибку очистки после неудачной активации, обновление отчёта после повторной cleanup и операцию, остановленную сразу после регистрации.

## Границы и следующий шаг

Реальные строки XLSX, изображения, JSON-отчёты и веса моделей в Git не добавлялись. Gate 3, реальные данные и запуск поиска по фотографии не входят в этот результат.

Следующий шаг — показать владельцу краткий итог Gate 2 и запросить отдельное owner approval. После этого можно решить, требуется ли merge ветки в `main`; push не является условием owner approval, но полезен как резервная копия и для последующего merge.

## Замечание по локальному Docker

Первая попытка интеграционного запуска использовала существующий контейнер `ai-database-1` и обнаружила несовпадение учётных данных старого тома. Том не удалялся и дальше не изменялся. Для проверки затем использовался отдельный Compose-проект и отдельный том. Перед следующим запуском основного локального контура стоит проверить его обычный env-файл и параметры `ai-database-1`.
