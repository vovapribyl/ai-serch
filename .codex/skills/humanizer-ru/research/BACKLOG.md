# Бэклог

Единый список открытых хвостов. Заведён потому, что наблюдения терялись между
релизами: замеченное в обсуждении не попадало ни в код, ни в записи.

Состояние на 2026-08-21.

## Данные, которых нет

| Пункт | Сейчас | Почему не закрыто |
| --- | --- | --- |
| Парные прогоны для `blind_eval.py` | 10 прогонов (2026-07-26 — 2026-08-11), судейство — слепые LLM-панели (владелец узаконил вместо человека), корпус с regex-маркерами собран | Человек-судья отменён решением владельца; новые оси — по мере появления артефактов |
| Реестр доказательств | 37 из 39 | 2 legacy-маркера остаются без immutable-источников; attached_file и grok_card закрыты как ненайденные публично |
| Проверочные образцы `tests/fixtures` | 5 файлов | Нужны жанры помимо уже покрытых |
| Реальные образцы машин в raw-корпусе | Закрыто 2026-08-11: покрыты все семь источников (GigaChat, Алиса, Le Chat, DeepSeek, Grok, Gemini, Copilot/Bing), 24 ответа с цепочками | Остаток вынесен в строку про Claude |
| Живой образец Claude (модель от 2026-08-02) для проверки водяного знака | Только заявление поставщика (пометка P-заявление, `llm-fingerprints.md`) | Граница исполнимости 2026-08-12: в рантайме агента нет маршрута Anthropic, а публичного верификатора знака не существует — образец добыть нечем. Замер по готовой процедуре выполняется, как только владелец вставит дословный ответ Claude (модель от 2026-08-02 и новее): скан A.7, прогон глубокой перезаписи, запись в реестр. Ветка «знак переживает перезапись» прописана заранее (SPEC_MERGED Т1): тогда для текстов Claude вводится явное правило, до появления верификатора — запись границы |

## Собственный стиль

Счётчик `count_style_markers.py` на наших же файлах после замены кружков словами:

| Файл | Всего нарушений |
| --- | --- |
| README.md | 200 |
| SKILL.md | 169 |
| CHANGELOG.md | 320 |

Часть из этого — шум разметки: счётчик считает табличные разделители и
технические скобки наравне с живым текстом. Режим `--skip-markup` существует
с 3.7.0 и снимает разметку; числа выше сняты без него и оставлены как
исторический ориентир. Запись здесь отставала от кода до 3.8.0.

### Замер 2026-08-06 со снятой разметкой

Числа сняты командой `python3 scripts/count_style_markers.py --skip-markup`;
полный вывод по 44 отслеживаемым `.md` хранится в журнале автономного прогона.
Три файла исторической таблицы дали такие числа:

| Файл | Всего нарушений |
| --- | --- |
| CHANGELOG.md | 286 |
| SKILL.md | 72 |
| README.md | 54 |

Справочники далеко впереди: у `references/chatbot-artifacts.md` 168, у
`references/false-positives.md` 165. Этого и ждать: они цитируют машинный
текст, и высокий счёт — назначение файла, а не дефект.

Предложенный порог: 90 нарушений на файл. Охват — проза поставки: `SKILL.md`,
`README.md`, `README.en.md`, `CONTRIBUTING.md`, `PERSONA.md`,
документация `eval/`, служебные файлы корня. Три причины не брать в порог
остальное:

- Справочники `references/` хранят цитаты машинных ответов; порог начнёт
  воевать с содержимым.
- `CHANGELOG.md` только дописывается: счёт растёт с каждым релизом, и
  фиксированное число однажды заставит себя поднять. Поднятие порога — решение
  владельца по свежему замеру, а не рутинная правка проверки.
- `research/` — история изысканий; фиксирует то, что было записано.

Запас над текущим максимумом 72 (`SKILL.md`) — четверть: легитимный рост
проходит, заметный дрейф не проходит. Реализация проверки в CI — отдельный
пункт бэклога.

- [x] `count_style_markers.py --skip-markup`: не считать синтаксис Markdown. Сделано в 3.7.0.
- [x] Порог на собственные файлы в CI — замер снят, предложение реализовано: `scripts/check_own_style.py` держит порог 90 на прозе поставки (2026-08-06).

## Структура и размеры

Порог предупреждения бюджета — 10000 токенов на справочник.

- [x] `references/chatbot-artifacts.md` — разбит на индекс и части (2026-08-06).
- [x] `references/test-fixtures.md` — разбит на индекс и части (2026-08-06).
- [ ] Разделение ядра и языкового слоя, чтобы скилл можно было перенести на другой язык.

## Отклонено до появления доказательств

Это не забытые пункты, а сознательные отказы.

- Regex для склеенных доменов GigaChat — нет независимого корпуса и отрицательных примеров.
- Отпечатки Алисы и YandexGPT — то же самое.
- Официальный `skills-ref validate` в CI — требует сетевого доступа в ранере.

## Правило ведения

Пункт попадает сюда в тот же день, когда замечен, даже если решение откладывается.
Замеченное в разговоре и нигде не записанное считается потерянным.
